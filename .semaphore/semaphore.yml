version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - checkout
        - name: 'Job #2'
          commands:
            - composer install
        - name: 'Job #3'
          commands:
            - vendor/bin/phpunit
